name: test img

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: nndda/another-test-image:latest
      credentials:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

    steps:
      - run: |
          ls -l /nnda.dev/
      - run: bun --version
      - run: python --version
      - run: |
          git clone https://github.com/nndda/nnda.dev.git --depth 1 && \
          cp -r ./* /nnda.dev/ && \
          cd /nnda.dev/ && \
          ls -l ./ && \
          source ./bin/activate

      - run: ls -l ./
      - run: bun run build
